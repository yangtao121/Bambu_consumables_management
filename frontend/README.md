# frontend（Next.js）

## 本地运行（不使用 Docker）

```bash
npm install
export NEXT_PUBLIC_API_BASE_URL="http://localhost:8000"
npm run dev -- --port 3000
```

## 说明

本前端以 Next.js App Router 实现基础页面与表单，后续可再引入 shadcn/ui 与 Tailwind 做更完整的视觉样式与 DataTable 组件化。


